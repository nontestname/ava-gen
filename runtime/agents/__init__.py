"""
Agents used by the AVA-Gen runtime.

For now we only plan a minimal ConversationAgent that:

- receives a Session + new user message
- updates history
- decides whether to ask clarification or return an ActionPlan
"""



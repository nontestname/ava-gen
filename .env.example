#
# AVA-Gen configuration example
#
# Copy this file to `.env` and fill in the values
# for your environment. The application will load
# these settings automatically.
#

# -------------------------------
# OpenAI configuration
# -------------------------------

# Required: your OpenAI API key
OPENAI_API_KEY=your_openai_api_key_here

# Optional: custom API base URL
# For standard OpenAI use https://api.openai.com/v1
# or set to a compatible proxy / Azure endpoint.
# OPENAI_BASE_URL=https://api.openai.com/v1

# Default model for AVA-Gen (optional)
# Falls back to gpt-4.1-mini if unset.
AVA_GEN_OPENAI_MODEL=gpt-4.1-mini

# Separate model for intent matching (optional)
# Defaults to AVA_GEN_OPENAI_MODEL when unset.
# AVA_GEN_INTENT_MODEL=gpt-4.1-mini


# -------------------------------
# Paths
# -------------------------------

# Workspace root for all per-app artifacts
# (input/, extracted_tests/, va_methods/, skills_description/, intent/, actionplan/)
AVA_GEN_WORKSPACE_ROOT=workspace

# Runtime data directory (sessions, logs, etc.)
AVA_GEN_RUNTIME_DATA_DIR=runtime/data

